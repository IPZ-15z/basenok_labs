const Beerman = require('./beerman');
const Ghost = require('./ghost');
const DEFAULTS = {
  bmanVel: 4,
  ghostVel: 2,
  gridSize: 40,
  wallSize: 4,
  lives: 2,
  bmanDir: [1, 0],
  ghostDir: [-1, 0]
};

